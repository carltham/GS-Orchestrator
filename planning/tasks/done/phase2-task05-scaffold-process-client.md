# Task 005: Scaffold ProcessClient Library (`lib/process-client/`)

## Phase
Phase 2: Runtime ProcessClient Library & Installer

## Objective
Scaffold package structure and export `IProcessAdapter` interface.

## Requirements
- `package.json` for `@gs/process-client`.
- `tsconfig.json`.
- `src/types/IProcessAdapter.ts` interface definition (`start`, `stop`, `getStatus`).

## Definition of Done
- `@gs/process-client` package builds cleanly and exports `IProcessAdapter`.
